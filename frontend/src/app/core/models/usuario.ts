export interface Usuario {
  idUsuario: number;
  nombreCompleto: string;
  fechaNacimiento: string;
  nacionalidad: string;
  correo: string;
  codigoArea: string;
  telefono: string;
  direccion: string;
  fechaRegistro: string;
  activo: boolean;
}
